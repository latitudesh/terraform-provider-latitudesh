---
page_title: "latitudesh_plan_vm Data Source - latitudesh"
subcategory: ""
description: |-
  Virtual machine plan data source - lookup a virtual machine plan by id, slug, or name.
---

# latitudesh_plan_vm (Data Source)

Look up a virtual machine plan by `id`, `slug`, or `name`. `slug` is the value expected by `latitudesh_virtual_machine.plan`.

## Example Usage

```terraform
data "latitudesh_plan_vm" "small" {
  slug = "vm-small"
}

output "plan_vm_available_operating_systems" {
  value = data.latitudesh_plan_vm.small.available_operating_systems
}

# `project` is inherited from the provider block:
#   provider "latitudesh" { project = "<project id or slug>" }
resource "latitudesh_virtual_machine" "example" {
  name             = "vm-01"
  plan             = data.latitudesh_plan_vm.small.slug
  site             = "ASH"
  operating_system = data.latitudesh_plan_vm.small.available_operating_systems[0]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) Virtual machine plan ID to look up.
- `name` (String) Virtual machine plan name to look up.
- `slug` (String) Virtual machine plan slug to look up. This is the value expected by `latitudesh_virtual_machine.plan`.

### Read-Only

- `available_operating_systems` (List of String) Operating system slugs compatible with this plan, as accepted by `latitudesh_virtual_machine.operating_system`.
- `disk` (Attributes) The plan's disk. (see [below for nested schema](#nestedatt--disk))
- `gpu` (String) The GPU type, if this plan includes a GPU.
- `memory` (Number) Total memory, in GB.
- `nics` (Attributes List) Network interface cards included in the plan. (see [below for nested schema](#nestedatt--nics))
- `regions` (Attributes List) Regions where this plan can be deployed, with per-region stock and pricing. (see [below for nested schema](#nestedatt--regions))
- `stock_level` (String) The overall stock level of the plan (`low`, `medium`, `high`, or `unavailable`).
- `vcpu` (Attributes) Detailed vCPU specifications. (see [below for nested schema](#nestedatt--vcpu))
- `vcpus` (Number) The number of virtual CPUs. Legacy field; prefer `vcpu.count`.
- `vram_per_gpu` (Number) VRAM per GPU, in GB.

<a id="nestedatt--disk"></a>
### Nested Schema for `disk`

Read-Only:

- `size` (Attributes) The disk size. (see [below for nested schema](#nestedatt--disk--size))
- `type` (String) The type of the disk (e.g., local SSD, local NVMe).

<a id="nestedatt--disk--size"></a>
### Nested Schema for `disk.size`

Read-Only:

- `amount` (Number) The total size of the disk.
- `unit` (String) The unit of the disk size (e.g. "gib").



<a id="nestedatt--nics"></a>
### Nested Schema for `nics`

Read-Only:

- `count` (Number) Number of NICs.
- `type` (String) NIC speed/type.


<a id="nestedatt--regions"></a>
### Nested Schema for `regions`

Read-Only:

- `available` (List of String) Sites in this region where the plan is offered.
- `locations` (Attributes) Sites in this region, broken down by availability. (see [below for nested schema](#nestedatt--regions--locations))
- `name` (String) Region name.
- `pricing` (Attributes Map) Prices for this region, keyed by ISO 4217 currency code (e.g. USD, BRL). (see [below for nested schema](#nestedatt--regions--pricing))
- `stock_level` (String) The stock level in this region (`low`, `medium`, `high`, or `unavailable`).

<a id="nestedatt--regions--locations"></a>
### Nested Schema for `regions.locations`

Read-Only:

- `available` (List of String) Sites with clusters that support this plan.
- `in_stock` (List of String) Sites with available capacity for this plan.


<a id="nestedatt--regions--pricing"></a>
### Nested Schema for `regions.pricing`

Read-Only:

- `hour` (Number) Hourly price.
- `month` (Number) Monthly price.
- `year` (Number) Yearly price.



<a id="nestedatt--vcpu"></a>
### Nested Schema for `vcpu`

Read-Only:

- `clock` (Number) The CPU clock speed, in GHz.
- `count` (Number) The number of virtual CPUs.
- `type` (String) The CPU type/model.
