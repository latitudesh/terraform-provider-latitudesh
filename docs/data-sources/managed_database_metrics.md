---
page_title: "latitudesh_managed_database_metrics Data Source - latitudesh"
subcategory: ""
description: |-
  Managed database metrics data source - retrieve a time window of metrics for a managed database (CPU, memory, TPS, connections, deadlocks, blocked queries, database size).
---

# latitudesh_managed_database_metrics (Data Source)

Retrieve a time window of metrics for a managed database: CPU usage, memory usage, TPS, max connections, deadlocks, blocked queries, and database size.

## Example Usage

```terraform
data "latitudesh_managed_database_metrics" "example" {
  managed_database_id = "managed_database_..."
  period              = 3600
  queries             = "cpuUsage,memoryUsage"
}

output "managed_database_metrics" {
  value = data.latitudesh_managed_database_metrics.example.metrics
}
```

`period` must be one of `1800`, `3600`, `21600`, `86400`, or `604800` seconds and defaults to `1800`. `queries` is a comma-separated subset of the available metric names and defaults to all of them.

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `managed_database_id` (String) Managed database ID to fetch metrics for.

### Optional

- `period` (Number) Time window in seconds. One of `1800`, `3600`, `21600`, `86400`, `604800`. Defaults to `1800`.
- `queries` (String) Comma-separated metrics to fetch. Defaults to all: `cpuUsage`, `memoryUsage`, `tpsUsage`, `maxConnections`, `deadlocks`, `blockedQueries`, `databaseSize`.

### Read-Only

- `from` (String) Start of the metrics time window (RFC3339).
- `metrics` (Attributes Map) Metrics keyed by metric name (e.g. `cpuUsage`). (see [below for nested schema](#nestedatt--metrics))
- `to` (String) End of the metrics time window (RFC3339).

<a id="nestedatt--metrics"></a>
### Nested Schema for `metrics`

Read-Only:

- `current` (Number) Most recent value of the metric. Null when the metric has no current sample.
- `points` (Attributes List) Time series samples for the metric, as returned by the API. (see [below for nested schema](#nestedatt--metrics--points))
- `unit` (String) Unit the metric is reported in.

<a id="nestedatt--metrics--points"></a>
### Nested Schema for `metrics.points`

Read-Only:

- `timestamp` (String) Sample timestamp (RFC3339).
- `value` (Number) Sample value.
