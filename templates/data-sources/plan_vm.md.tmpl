---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

Look up a virtual machine plan by `id`, `slug`, or `name`. `slug` is the value expected by `latitudesh_virtual_machine.plan`.

## Example Usage

{{ tffile (printf "examples/%s.tf" .Name) }}

{{ .SchemaMarkdown | trimspace }}
